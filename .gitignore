.venv/
__pycache__/
node_modules/
dist/
.DS_Store


# The customers' own quotations and the throwaway parsing of them. Anchored to
# the root so the app's own scripts, which are code, are kept.
/data/
/scripts/

temp/
